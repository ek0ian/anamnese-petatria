// js/app.js - mostra apenas a view de login por padrao no commit do layout.
// A navegacao e logica completa entram nos proximos commits.
document.addEventListener("DOMContentLoaded", () => {
    document.getElementById("view-login").classList.add("ativa");
});
